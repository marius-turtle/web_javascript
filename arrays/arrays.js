//Create an Array

//Traverse the created Array and log the values on each index using a loop of your choice

//Use the Array.map()-method to create a new Array only containing the fornames
//HINT: can be solved with some methods from the String.prototype
let names = ['Peter Lustig', 'Max Mustermann', 'Donald Duck', 'Harry Potter'];

//sort the names Array A-Z
let names = ['Peter', 'Max', 'Donald', 'Harry'];

//sort the names Array Z-A
//HINT: you can use an own comparator-function or another method from Array.prototype we got to know
let names = ['Peter', 'Max', 'Donald', 'Harry'];

//filter the Array prices for prices lower than 30$
let pricesInDollar = [9.99, 19.99, 100, 15, 17.89, 101.47, 999.99];

//reduce the Array stocks to get the value of the complete portfolio
//HINT you can access the price property of one entry like 
//stockPortfolio[0].price //9.99
let stockPortfolio = [
    { price: 9.90, amount: 100 },
    { price: 19.80, amount: 50 },
    { price: 100, amount: 100 },
    { price: 15, amount: 200 },
    { price: 17.80, amount: 10 },
    { price: 101.40, amount: 20 },
    { price: 809.9, amount: 10 }
]

//use the sort() and reverse()-method to sort the stock-prices in descending order
let pricesInDollar = [9.99, 19.99, 100, 15, 17.89, 101.47, 999.99];
